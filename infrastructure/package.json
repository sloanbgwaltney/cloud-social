{
  "name": "infrastructure",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "sst test",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove"
  },
  "devDependencies": {
    "@aws-cdk/assert": "1.79.0"
  },
  "dependencies": {
    "@serverless-stack/cli": "0.4.4",
    "@serverless-stack/resources": "0.4.4",
    "@aws-cdk/core": "1.79.0"
  }
}
